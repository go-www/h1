name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.17

    - name: Build
      run: go build -v -gcflags="-m" ./...

    - name: Test
      run: go test -v ./...
    
    - name: Make Report
    - id: make-report
      run: |
          go install github.com/lemon-mint/golang-ci-tools@latest
          golang-ci-tools > report.md
          body=$(cat report.md)
          body="${body//'%'/'%25'}"
          body="${body//$'\n'/'%0A'}"
          body="${body//$'\r'/'%0D'}" 
          echo ::set-output name=body::$body
    - name: commit-comment
      uses: peter-evans/commit-comment@v1
      with:
        body: ${{ steps.make-report.outputs.body }}

    - name: Run Benchmark
    - id: make-benchmark-report
      env:
        IMGBB_API_KEY: ${{ secrets.IMGBB_API_KEY }}
      run: |
          body=$(go run ./.github/test)
          body="${body//'%'/'%25'}"
          body="${body//$'\n'/'%0A'}"
          body="${body//$'\r'/'%0D'}" 
          echo ::set-output name=body::$body

    - name: commit-comment
      uses: peter-evans/commit-comment@v1
      with:
        body: ${{ steps.make-benchmark-report.outputs.body }}

